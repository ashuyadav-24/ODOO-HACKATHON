"use client";
import Link from "next/link"; import { usePathname, useRouter } from "next/navigation"; import type { ReactNode } from "react";
const links = [["Overview", "/dashboard", "◫"], ["Assets", "/assets", "▣"], ["Organization", "/organization", "♙"]];
export default function AppShell({ children }: { children: ReactNode }) { const pathname = usePathname(); const router = useRouter(); const logout = () => { localStorage.removeItem("assetflow_token"); router.push("/login"); }; return <div className="app-shell"><aside className="sidebar"><Link className="brand" href="/dashboard"><span>◆</span> AssetFlow</Link><p className="nav-label">WORKSPACE</p><nav>{links.map(([label, href, icon]) => <Link key={href} href={href} className={pathname === href ? "nav-link active" : "nav-link"}><i>{icon}</i>{label}</Link>)}</nav><div className="sidebar-bottom"><div className="help-card"><span>✦</span><div><b>Need help?</b><small>Check the quick guide</small></div></div><button className="logout" onClick={logout}>↪ Sign out</button></div></aside><main className="workspace"><header className="topbar"><div className="search">⌕ <input aria-label="Search" placeholder="Search assets, people..." /></div><div className="user"><span className="notification">♧</span><span className="avatar">AM</span><div><b>Alex Morgan</b><small>Administrator</small></div></div></header>{children}</main></div>; }
